# Simple Shell
A UNIX command-line interpreter built from scratch in C.
Supports command execution, PATH resolution, built-ins (exit, env), and piping.
Built as part of low-level systems programming coursework.

## Build
gcc -Wall -Werror -Wextra -pedantic *.c -o hsh

## Usage
./hsh
